# Hidden tests boundary

No learner-visible hidden fixtures are included in this prototype artifact.

Production hidden tests must be stored/deployed separately from learner-accessible course source and mounted read-only into an ephemeral test sandbox. The web process must never import or execute learner code.
