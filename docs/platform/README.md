# Platform Design and Build Documents

1. `../../PROJECT_DECISIONS.md` — consolidated decision register v1.1 baseline.
2. `../../PROJECT_DECISIONS_v1.2.md` — authoritative v1.2 delta; §8 contains the post-Codex evidence qualification and supersedes broader v0.4 verification wording where they conflict.
3. `INTERACTION_WIREFRAME_SPEC_v1.0.md` — interaction and wireframe contract for the Causal Attention vertical slice.
4. `WEB_PLATFORM_TECHNICAL_ARCHITECTURE_v1.0.md` — implementation architecture and security boundaries.
5. `CAUSAL_ATTENTION_WEB_PROTOTYPE_v0.1.md` — initial vertical-slice scaffold status.
6. `PERSISTENCE_BACKED_EVIDENCE_FLOW_v0.2.md` — durable evidence and persistence increment.
7. `REAL_SUBMISSION_EXECUTION_v0.3.md` — real submission/execution release gates and security invariants.
8. `HIDDEN_EVALUATOR_PROTOCOL_v0.3.md` — split learner-probe/private-evaluator transport contract.
9. `CAUSAL_ATTENTION_ADAPTER_v1.md` — executable runtime adapter ABI for `phase1-causal-attention@1.0`.
10. `CAUSAL_ATTENTION_MEMORY_TRACE_v2.md` — regression-tested structural evidence contract for `attention.no_permanent_kv_repeat`; supersedes the adapter document's earlier schema-1 memory-trace section where they conflict.
11. `RUNTIME_IMAGE_PROVENANCE_v0.3.md` — base-image pinning, OCI/FPLLM labels, CI provenance artifacts, and the registry-publication evidence boundary.
12. `STAGING_RELEASE_GATES_v0.3.md` — exact S1–S6 evidence required before the real-worker v0.3 release can leave draft status.
13. `STAGING_SUBMISSION_E2E_v0.3.md` — guarded GitHub Actions harness for the real GitHub App -> durable job -> worker -> Docker -> persisted-evidence release gate.
14. `REAL_LEARNER_IDENTITY_v0.4.md` — merged v0.4 identity/onboarding baseline with post-release qualification of what the frozen release evidence did and did not verify.
15. `PLATFORM_PRODUCTION_BETA_v0.5.md` — production-beta contract, P0–P7 release gates, operations/security requirements, and external learner validation protocol.
16. `PRODUCTION_PROVIDER_DECISIONS_v0.5.md` — linked Platform v0.5 P0 infrastructure/operations decision record: AWS `eu-west-1`, ECS Express/Fargate web, PostgreSQL 18, EC2 + gVisor worker/sandbox, ECR, CloudWatch/X-Ray, Secrets Manager, AWS-managed production origin/TLS, restore targets, provider quotas, and compatibility-gated release/rollback process. Details corrected by items 19–48 where they conflict.
17. `GITHUB_PROVIDER_LIMITS_v0.5.md` — P0 GitHub identity/repository/source provider contract: data/region and cost implications, exact human OAuth and GitHub App permissions/secrets, published REST/App limits, bounded source-tree/materialization envelope, provider-aware defer/backoff behavior, and exit path. Admission and deferral semantics are tightened by items 19–48.
18. `GITHUB_ACTIONS_WEBHOOK_PROVIDER_v0.5.md` — P0 companion for GitHub Actions production release orchestration and GitHub App installation webhooks: standard hosted runner model, OIDC trust/role boundary, sensitive evaluator CodeBuild boundary, webhook URL/event/secret/rotation/idempotency contract, costs/limits, and failure/exit behavior. OIDC/release/webhook details are corrected by items 19–48.
19. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_v0.5.md` — first authoritative maintainer-review correction delta over items 16–18. It fixes the invalid RDS class, immutable GitHub OIDC subject, hidden-evaluator release chain, worker-controller topology, image-baked test distribution, whole-tree source preflight, webhook authority/idempotency semantics, bounded request bodies, ECS Express SG semantics, cost envelope, worker drain, executable gVisor selection, release source identity, and CDK Express capability gate.
20. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R2_v0.5.md` — second authoritative maintainer-review correction delta. It supersedes conflicting P0 text with the corrected M8gd source, explicit trusted-worker egress policy, exact ECR role/repository boundaries, sensitive CodeBuild cache/log/artifact hygiene, and durable GitHub provider-deferral semantics separated from evaluator retry accounting.
21. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R3_v0.5.md` — third authoritative maintainer-review correction delta. It removes direct GitHub-controlled sensitive-builder and privileged-worker image authority, introduces protected AWS-side builders/release broker/candidate state, private sensitive-build egress, protected worker promotion, and the associated authorization/integrity evidence gates.
22. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R4_v0.5.md` — fourth authoritative maintainer-review correction delta. It removes the last GitHub-built executable image from the hidden-evaluator trust chain by adding a protected evaluator-base builder sourced from an independently verified execution context, moves evaluator-base ECR write authority into the protected boundary, expands broker/candidate state to three protected components, and explicitly records GitHub Actions as an accepted web/control-plane release TCB rather than claiming isolation from every production secret-bearing runtime.
23. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R5_v0.5.md` — fifth authoritative maintainer-review correction delta. It resolves the post-round-4 candidate-lifecycle contradiction by making protected output digests broker-written post-approval state, freezes the private-material publisher, sensitive-release approver, and protected-deploy roles as separate non-GitHub federated authorities, and prohibits protected promotion from substituting non-recorded digests.
24. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R6_v0.5.md` — sixth authoritative correction delta produced from the fresh exact-HEAD Codex review. It removes every final-hidden-evaluator ECR permission from the networked evaluator-base builder and replaces direct approver writes to shared candidate state with a protected schema-enforcing admission Lambda, server-generated initial candidate state, phase-separated field ownership, and canonical-state promotion checks.
25. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R7_v0.5.md` — seventh authoritative maintainer-first correction delta. It freezes one transactional active-candidate slot so GitHub cannot ambiguously select among approvals, defines immutable protected S3 build-result receipts plus CodeBuild/ECR reconciliation before broker-owned digests become canonical, and corrects admission-actor attribution by making CloudTrail Lambda `Invoke` data events plus the invocation receipt the canonical federated-actor evidence.
26. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R8_v0.5.md` — eighth authoritative maintainer-first correction delta. It globally binds each broker request ID to one approval so replay cannot migrate to a later candidate, adds pre-approval admission idempotency with payload-hash conflict detection, and freezes the successful promotion transaction that marks the candidate promoted and releases the active slot only after canonical infrastructure identity is smoke/reconciled.
27. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R9_v0.5.md` — ninth authoritative maintainer-first correction delta. It globally binds each promotion request ID to one approval/canonical digest snapshot, atomically claims one active promotion identity per candidate before infrastructure mutation, defines same-ID replay and different-ID concurrency behavior, and freezes terminal success/rollback/uncertain-state transitions so a promotion replay cannot migrate to a later release.
28. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R10_v0.5.md` — tenth authoritative maintainer-first correction delta. It closes the non-transactional CodeBuild start gap by reserving one durable logical build attempt before `StartBuild`, using CodeBuild's native idempotency token plus bounded non-secret attempt correlation, recovering accepted-but-unrecorded builds before any later start, disabling provider-side automatic retries, and binding result receipts to the exact logical attempt.
29. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R11_v0.5.md` — eleventh authoritative correction delta produced from the fresh exact-HEAD Codex review of R10. It makes every unresolved logical attempt—including `starting` before a build ID exists—block candidate supersession, adds a final active-candidate/attempt check before each external `StartBuild`, records the immutable prior production state (`PAIR` or initial-release `ABSENT`) at promotion claim, retains prior images through terminal rollback disposition, and freezes a protected recovery mediator with bounded target-reconcile and exact-recorded-prior-state rollback transitions out of `failed_requires_operator`.
30. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R12_v0.5.md` — twelfth authoritative correction delta produced from the fresh exact-HEAD Codex review of R11. It fences every external `StartBuild` with a durable dispatch epoch/lease that blocks attempt/candidate-invalidating transitions until provider reconciliation, introduces a protected execution-deployment generation singleton and exclusive mutation lock, and replaces direct human protected-stack mutation plus the separate recovery mediator with one schema-enforcing promotion controller that can apply/recover only the bound target or exact recorded prior `PAIR`/`ABSENT` state.
31. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R13_v0.5.md` — thirteenth authoritative maintainer-first correction delta. It closes the controller's own accepted-but-unrecorded provider-call gap with a durable mutation journal and exact launch-template/ASG/old-instance reconciliation protocol, makes rollback direction monotonic, freezes the pre-first-release `ABSENT` worker bootstrap as ASG `0/0/1` before steady-state `1/1/1`, and separates routine invoke-only protected deployment from an explicit non-GitHub high-privilege protected-stack administrator used only for reviewed infrastructure/controller changes.
32. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R14_v0.5.md` — fourteenth authoritative maintainer-first hardening delta. It makes exact numeric prior/bootstrap launch-template identities part of the promotion snapshot and requires rollback to reuse them rather than synthesize equivalents; freezes source-mode-specific drain/mutation ordering so every serving `PAIR` is drained before ASG capacity/runtime selection can terminate or replace it; and serializes normal protected-stack administration against active build/promotion/recovery state, with explicit incident reconciliation for emergency break-glass use.
33. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R15_v0.5.md` — fifteenth authoritative maintainer-first hardening delta. It turns worker drain into durable global leasing control with a promotion-bound drain epoch and worker acknowledgement/zero-active-lease proof, keeps replacement workers fail-closed while a release mutation is active, binds leasing re-enable to the terminal release transaction, and distinguishes the pristine-prior first-mutation fence from exact journaled intermediate-state recovery after provider mutation has begun.
34. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R16_v0.5.md` — sixteenth authoritative maintainer-first correction delta. It closes the first-release leasing contradiction by freezing bootstrap `ABSENT` as `DRAINED`, making every promotion that can produce a serving `PAIR` claim one promotion-bound service-control epoch even when no source worker exists, requiring source acknowledgement only for a serving source `PAIR`, and keeping first-release target workers non-leasing until the exact terminal promotion transaction enables service.
35. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R17_v0.5.md` — seventeenth authoritative maintainer-first correction delta. It removes the stale optional worker scale-to-zero pause path that no longer has a bounded mutation authority under the protected controller model: after first promotion, ordinary production remains ASG `1/1/1` even during learner-facing pauses; `ABSENT` is bootstrap/first-rollback only; and any future steady-state scale-to-zero mechanism requires an explicit P0 amendment.
36. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R18_v0.5.md` — eighteenth authoritative maintainer-first correction delta. It separates initial P1 protected-stack bootstrap from post-promotion administration and removes the undefined maintenance rewrite path for live worker deployment identity: after first promotion, normal stack-admin changes must leave the current worker ASG/LT/runtime/capacity and release-control identity unchanged; a planned live worker-substrate change requires a P0 amendment, while emergency break-glass remains incident-only and cannot manufacture normal release state.
37. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R19_v0.5.md` — nineteenth authoritative maintainer-first provider-limit delta. It closes quota coverage for the protected release architecture by freezing all three protected CodeBuild projects to serialized `LINUX_CONTAINER + BUILD_GENERAL1_LARGE`, requiring at least one applied Linux/Large build slot, recording the three one-unit protected Lambda reservations, bounding release-control DynamoDB to at most two on-demand tables, recording the sensitive-builder interface-endpoint envelope, and extending the first P1 read-only no-create admission report to these services.
38. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R20_v0.5.md` — twentieth authoritative maintainer-first serialization delta. It makes R19's one-CodeBuild-slot envelope executable by adding one candidate-wide protected-build slot claimed atomically with logical-attempt reservation, keeping all other protected components blocked through `starting`, dispatch ambiguity, running build, and result reconciliation, and requiring quota/throttle recovery to stay within the same reserved logical attempt/idempotency identity until durable disposition.
39. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R21_v0.5.md` — twenty-first authoritative correction delta produced from the fresh exact-HEAD Codex review of R20. It fences every new PostgreSQL learner-lease acquisition with a durable protected acquisition slot that serializes against drain and adds a durable protected-stack maintenance epoch/lock claimed atomically from fully quiescent release-control state and checked by every release-control authority grant.
40. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R22_v0.5.md` — twenty-second authoritative correction delta produced from the fresh exact-HEAD Codex review of R21. It makes learner lease acquisition one server-bounded autocommit database operation, adds a protected maintenance controller plus provider-dispatch lease and exact no-start invalidation path, and generalizes service-control ownership so runtime-affecting maintenance has an explicit maintenance-owned drain/acknowledgement/re-enable protocol.
41. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R23_v0.5.md` — twenty-third authoritative correction delta produced from the fresh exact-HEAD Codex review of R22. It removes new-lease database authority from the long-lived worker by adding a bounded lease-acquisition controller with an acquisition dispatch lease and dedicated database privilege boundary, and it adds durable maintenance `INCIDENT` mode so failed/uncertain maintenance cannot clear into `IDLE` while service-control ownership or provider reconciliation remains unresolved.
42. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R24_v0.5.md` — twenty-fourth authoritative correction delta produced from the fresh exact-HEAD Codex review of R23. It adds durable break-glass incident admission for emergencies that do not originate from normal maintenance, a broker-only short-lived emergency provider role, monotonic incident-epoch fencing for release work that won a prior race, session-expiry/revocation requirements before incident exit, and a sixth protected one-unit control-Lambda reservation.
43. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R25_v0.5.md` — twenty-fifth authoritative maintainer-review correction delta over R24. It fixes the incident-epoch predicate so normal maintenance remains executable while `ACTIVE`, pre-reserves and dispatch-fences every emergency STS session attempt so accepted-but-unrecorded credentials cannot permit early incident exit, constrains emergency credentials with server-owned repair-profile session policies, and gives protected audit detection a durable `AUDIT_DETECTED` incident-admission action without adding another control Lambda.
44. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R26_v0.5.md` — twenty-sixth authoritative maintainer-review timing correction delta. It fixes the ambiguous-session expiry bound at the dispatch transaction, freezes emergency STS duration to the provider-valid exact 900 seconds, prevents emergency repair credentials from racing overlapping pre-incident provider mutations, and makes `AUDIT_DETECTED` incident origin/replay identity explicit.
45. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R27_v0.5.md` — twenty-seventh authoritative maintainer-review provider-limit delta. It corrects the emergency IAM role to provider-valid `MaxSessionDuration=3600` while preserving broker-requested `DurationSeconds=900`, freezes a bounded inline-session-policy/tag/session-name envelope below STS limits, and requires real provider validation of every deployed repair profile without truncation or broadened fallback.
46. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R28_v0.5.md` — twenty-eighth authoritative correction delta produced from the fresh exact-HEAD Codex review of R27. It binds learner lease/result/evidence terminalization to the protected incident epoch and freezes the initial v0.5 mutating emergency repair-profile registry as empty: the incident broker admits/fences incidents but cannot mint provider-mutation credentials, and any unmodeled provider repair requires an explicit P0 amendment.
47. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R29_v0.5.md` — twenty-ninth authoritative maintainer-first correction delta. It closes R28's cross-store TOCTOU by adding a PostgreSQL-local learner incident-fence singleton, crash-recoverable protected-to-database fence establishment, database-local serialization for lease acquisition/renewal and learner evidence/mastery terminalization, and a bounded incident-quarantine disposition that clears stale leases without learner consequence.
48. `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_R30_v0.5.md` — thirtieth authoritative correction delta produced from the independent exact-head quota-fallback second opinion of R29. It makes PostgreSQL `OPEN -> RESERVED` the first learner-side incident-admission fence, serializes all learner-fencing incident origins through one exact admission mediator without adding a seventh Lambda, finalizes `RESERVED -> FENCED` after protected incident allocation, and requires every ordinary learner ownership/evidence/mastery mutation that contends on the fence to be one server-bounded autocommit operation.

Decision precedence is: v1.2 delta over v1.1 where they conflict; otherwise v1.1 remains in force. Within v1.2, the post-review qualification in §8 governs evidence claims. The linked Platform v0.5 P0 provider/operations record is the thirty-three-document set `PRODUCTION_PROVIDER_DECISIONS_v0.5.md` + `GITHUB_PROVIDER_LIMITS_v0.5.md` + `GITHUB_ACTIONS_WEBHOOK_PROVIDER_v0.5.md` + `PRODUCTION_PROVIDER_MAINTAINER_CORRECTIONS_v0.5.md` + rounds R2–R30. Within that set, R30 has highest precedence, then R29 through R2 in descending order, then the first maintainer-corrections document, where text conflicts. Together the thirty-three documents supersede only v1.2 §1.4 item 4's pre-P0 statement that production provider selection was still open; runtime subcontracts may narrow implementation details but must not weaken frozen security or evidence invariants.

PR #10 remains under the maintainer-first merge gate. The independent exact-head quota-fallback second opinion of `afdc98ba...` found FPR30-01: R29 could still allow a fresh or unbounded ordinary learner terminalization to win the PostgreSQL fence ordering after protected incident admission but before the database fence was established. R30 closes that decision-level gap by reserving the database learner fence first (`OPEN -> RESERVED`), then committing protected `INCIDENT`, then finalizing the exact database incident identity (`RESERVED -> FENCED`), while requiring every ordinary learner mutation that contends on the fence to be one bounded autocommit server operation. R28's empty mutating-emergency registry/no emergency provider role remains in force. The latest exact candidate must pass CI, receive a fresh exhaustive maintainer exact-HEAD re-review, and receive a fresh independent exact-head second opinion (Codex when available, quota-fallback review otherwise) before final thread disposition and squash merge.