import Link from "next/link";import {Badge,Card} from "@fpllm/ui";import {getExperimentByDisplayIdForDemo,getDemoUser,prisma} from "@fpllm/db";
export const dynamic="force-dynamic";
export default async function JournalEntryPage({params}:{params:Promise<{displayId:string}>}){const {displayId}=await params;const [exp,user]=await Promise.all([getExperimentByDisplayIdForDemo(displayId),getDemoUser()]);if(!exp)return <p>Experiment not found.</p>;const compute=await prisma.computeProfile.findFirst({where:{userId:user.id},orderBy:{createdAt:"desc"}});const version=exp.journalEntry?.versions[0];return <><header className="page-header"><p className="eyebrow">Research Journal · {exp.displayId}</p><h1>Attention Memory Scaling</h1><p>System evidence and learner-authored analysis remain separate.</p><div className="page-actions"><Badge tone="research">{version?"Finalized":"Awaiting interpretation"}</Badge></div></header><Card><p className="eyebrow">Hypothesis · locked pre-run</p><p>{exp.hypothesis??"—"}</p></Card><Card><p className="eyebrow">Prediction · locked pre-run</p><p>{exp.prediction??"—"}</p></Card><Card><p className="eyebrow">Configuration · system evidence</p><div className="table-wrap"><table><tbody><tr><th>Commit</th><td className="mono">{exp.commitSha.slice(0,7)}</td></tr><tr><th>Profile</th><td>{compute?.selectedProfile??"—"}</td></tr><tr><th>GPU</th><td>{compute?.detectedGpu??"—"}</td></tr></tbody></table></div></Card>{version?<Card><p className="eyebrow">Interpretation · learner analysis · version {version.version}</p><p>{version.interpretation}</p><p className="small muted"><strong>Uncertainty:</strong> {version.uncertainty}</p></Card>:<Card className="emphasis"><p className="eyebrow">Interpretation required</p><Link className="button primary" href={`/experiments/attention-memory-scaling/${exp.displayId}`}>Open results</Link></Card>}</>}
